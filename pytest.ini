[pytest]
testpaths = tests
pythonpath = src
addopts = -v --strict-markers
asyncio_mode = auto
markers =
    unit: Testes unitários isolados
    integration: Testes de integração com mocks de API
    security: Testes de validação de segurança e regras OWASP/STRIDE
